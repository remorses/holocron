---
'@holocron.so/vite': minor
---

Add a `pageCache` plugin option. Set `pageCache: false` to skip reuse of processed navigation and MDX during Holocron development. Image metadata still caches. The default remains `true` for published sites.
