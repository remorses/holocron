---
'@holocron.so/vite': patch
---

Tone down AI chat tool-call chrome.

Tool rows now use Claude Code's `└` tree glyph, smaller muted monospace, and tighter spacing between consecutive calls so they sit behind the answer instead of competing with it.
