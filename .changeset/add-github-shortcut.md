---
'@holocron.so/vite': minor
---

<!-- Adds the automatic GitHub repository shortcut. -->

Add a built-in `/github` shortcut when the site config links to GitHub from the navbar, navigation, logo, or footer.

The temporary redirect preserves the configured URL. An authored `github.mdx` page or an explicit redirect rule that matches `/github` takes priority.
