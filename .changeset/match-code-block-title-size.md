---
'@holocron.so/vite': patch
---

Match code block titles to the code font size.

Filename and language labels above fenced blocks now use `--type-code-size`, the same token as the code itself, so the title no longer looks larger than the code.
