---
'@holocron.so/vite': patch
---

Stop showing the fence language as a code block title when no title is set.

Untitled fences now render without a header. Pass an explicit title (for example ` ```ts filename.ts `) when you want a label above the block.
