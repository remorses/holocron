---
'@holocron.so/vite': patch
---

Callout colors now follow your brand `colors.primary`. `Note` uses the primary color, `Info` is neutral gray (matching Mintlify), and `Tip`, `Check` and `Warning` shift their hue slightly toward the primary so they never clash with it, for example a green brand next to a green `Tip`. `Danger` stays red.
