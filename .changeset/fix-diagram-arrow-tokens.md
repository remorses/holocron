---
'@holocron.so/vite': patch
---

Color diagram arrowheads as connectors, not labels.

Unicode arrows such as `►` `◄` `▼` `▲` in ` ```diagram ` fences now use the same structural token as ASCII arrows (`-->`), so they follow the diagram connector color instead of the label color.
