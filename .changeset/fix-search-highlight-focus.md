---
'@holocron.so/vite': patch
---

Hide the sidebar search active-row highlight when the search input is not focused.

The keyboard-navigation highlight (arrow up/down) used to stay painted on a row even after the search input lost focus. It now only shows while the search input is focused.
