---
'@holocron.so/vite': patch
---

Replace the Holocron wordmark JPEG with an inline SVG in AI chat, login, and the dashboard.

The logo now inherits `currentColor`, so it follows light and dark text without blend-mode or invert hacks.
