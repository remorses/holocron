---
'@holocron.so/vite': minor
---

Add `sidebar.animate` config field to enable left navigation sidebar transitions.

```json
{
  "sidebar": {
    "animate": true
  }
}
```

When enabled, expand/collapse and hover transitions are applied to the sidebar navigation tree. The `sidebar` object is extensible for future sidebar settings.
