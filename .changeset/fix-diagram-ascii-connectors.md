---
'@holocron.so/vite': patch
---

Color ASCII diagram connectors `^` `+` `/` `\` the same as arrows and box lines.

In ` ```diagram ` fences those characters are structural, like `>` `<` and `-->`, not labels.
