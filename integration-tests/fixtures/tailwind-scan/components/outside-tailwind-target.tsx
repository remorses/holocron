/** Component imported from outside the Tailwind scan fixture pagesDir. */

export function OutsideTailwindTarget() {
  return <div id="tw-outside-component-target">This outside component gets Tailwind classes during HMR.</div>
}
